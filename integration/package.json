{
  "name": "@lessonkit/integration",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node -e \"if (+process.versions.node.split('.')[0]<20){console.log('@lessonkit/integration: skipped on Node '+process.versions.node+' (requires 20+)');process.exit(0)}\" && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
