# Interactive Video example

Demonstrates `@lessonkit/react` **1.4.0** blocks: `InteractiveVideo`, `TimedCue`, `Video`, `MemoryGame`, and `InformationWall`.

```bash
# From repo root (after npm run build:packages)
npm -w lessonkit-example-interactive-video run dev
```

Packaging requires Node **18+**.
